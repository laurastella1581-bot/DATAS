import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 1. 학습에 사용했던 피처 이름 리스트 (target 컬럼 제외)
features = [col for col in train.columns if col not in ['count', 'casual', 'registered', 'datetime']]

# 2. 피처 중요도를 판다스 Series로 변환 및 정렬
imp_series = pd.Series(model.feature_importances_, index=features).sort_values(ascending=False)

# 수치 확인
print(imp_series)

# 3. 가로 막대 그래프로 시각화
plt.figure(figsize=(10, 6))
sns.barplot(x=imp_series.values, y=imp_series.index)
plt.title("LightGBM Feature Importances")
plt.xlabel("Importance")
plt.ylabel("Features")
plt.show()